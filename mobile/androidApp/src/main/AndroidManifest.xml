<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required to request and bind a foreground test to the selected transport. -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required by Android to read the connected Wi-Fi name. Requested only from
         History, and only to label measurements with the network they were taken
         on. Coarse is declared alongside fine because Android 12+ lets the user
         grant approximate location only; requesting fine without coarse is a
         lint error and leaves the approximate grant unhandled. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Reschedule the periodic work after a reboot; without this the monitor
         silently stops until the user next opens the app. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Report delivery. Runtime-requested on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Keeps a manual live test visible and running while the app is
         minimized. Android 14+ additionally requires a Play Console
         declaration form for the dataSync type before this can ship to the
         Play Store; that is a store-listing requirement, not a runtime gate,
         so it does not block a sideloaded/debug build. Android 15 caps
         dataSync at 6h of cumulative foreground time per 24h — an
         "Unlimited"-duration manual test can in principle hit that, at which
         point the OS itself throws and the run ends; a bounded per-phase
         duration never gets close. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".MonitorApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SevenMonitor">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exports are written to a cache file and shared by content URI.
             Putting a 90-day CSV in Intent.EXTRA_TEXT risks Android's ~1MB
             Binder transaction limit, which fails as a crash rather than as a
             handled error. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".work.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".work.LiveTestForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
