<?xml version="1.0" encoding="utf-8"?>
<!--
  The one directory the export FileProvider may share from.

  Scoped to a single subdirectory of the app's cache rather than the whole of
  it, so a future cache write cannot accidentally become shareable.
-->
<paths>
    <cache-path name="exports" path="exports/" />
</paths>
